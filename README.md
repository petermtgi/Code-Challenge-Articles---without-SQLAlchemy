# Articles Code Challenge

## Setup

1. Run the database setup:
    ```
    python scripts/setup_db.py
    ```
2. Seed the database:
    ```
    python -m lib.db.seed
    ```
3. Run tests:
    ```
    pytest tests/
    ```
